# Leave empty to run fully standalone with the built-in local mock data layer.
# Set to your Spring Boot backend to use the real API (yudao /app-api dialect), e.g.:
# VITE_API_URL=http://localhost:8080
VITE_API_URL=
